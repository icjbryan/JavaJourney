
public class LoopsActivity {

	public static void main(String[] args) {
		/*Loops Activity
		In the doubleArray method of the StudentCode class, 
		use a for loop to go through the numeric items in the 
		numbers array variable being passed in as a parameter. 
		For each numeric item in numbers, double it and add the 
		result as a new item in the results array.

		For instance, if the number array contained { 1, 2, 3 }, 
		the results array should be { 2, 4, 6 } as each number was doubled.*/
		
		
	}

}
