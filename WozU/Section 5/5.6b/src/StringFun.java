
public class StringFun {
	//Static classes have the keyword static appended after the access modifier. 
	//They do not require a constructor, meaning the method can be called directly from the class. 
	public static String shout(String input) {
		String temp = input.toUpperCase();
		return temp;
	}
}
